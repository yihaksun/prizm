import { ResourceManagementWorkspace } from '@/components/resource-management-workspace';

export default function ExecutionEnvironmentsPage() {
  return <ResourceManagementWorkspace mode="environment" />;
}
