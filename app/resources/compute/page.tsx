import { ResourceManagementWorkspace } from '@/components/resource-management-workspace';

export default function ExecutionResourcesPage() {
  return <ResourceManagementWorkspace mode="compute" />;
}
